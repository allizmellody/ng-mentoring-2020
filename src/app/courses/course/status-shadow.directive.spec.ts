import { StatusShadowDirective } from './status-shadow.directive';

describe('StatusBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusShadowDirective();
    expect(directive).toBeTruthy();
  });
});
