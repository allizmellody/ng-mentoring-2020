import { AutosuggestInputDirective } from './autosuggest-input.directive';

describe('AutosuggestInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosuggestInputDirective();
    expect(directive).toBeTruthy();
  });
});
